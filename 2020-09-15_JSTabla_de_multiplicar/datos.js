
let result= 0;
for (var x = 0; x < 11; x++) {
    for (var y = 0; y < 10; y++){
        result = (x * y);  
        document.write("El resultado de  " + x + " x " + y + " = " + "<h1>" + result + "</h1>") 
    } 
}